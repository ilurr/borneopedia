<div class="post-inline" id="{{type}}AnswerForm{{@index}}">
	<div class="post-inline-wrap">
		<form action="{{type}}.html">
			<div class="post-inline-content">
				<textarea name="{{type}}-textarea-answer" id="{{type}}-textarea-answer{{@index}}" class="tinymce-inline"></textarea>
			</div>
			<div class="post-inline-footer">
				<button class="button inline-button-draft" disabled="disabled" id="{{type}}-button-answer{{@index}}-draft">Save to draft</button>
				<button class="button -primary inline-button-post" disabled="disabled" id="{{type}}-button-answer{{@index}}">POST</button>
			</div>
		</form>
	</div>
</div>
