<!DOCTYPE html>
<html lang="en">

{{>base/head font="link"}}

<body>
	<!-- s: root -->
	<div class="root">

		{{>layout/header login=true}}
		
		<!-- grid -->
		<div class="grid">

			<div class="grid-flex">

				<div class="grid-col -nav">
					<div class="nav-fixed">
						{{>layout/nav logged=true active="Setting"}}

						{{>layout/footer footer="small"}}
					</div>
				</div>

				<div class="grid-col -wide">

					<!-- setting -->
					<div class="setting box">
						<h2 class="title">Change Email</h2>
						<form action="setting.html">
							<div class="user-form">
								<div class="user-form-row">
									{{>component/input type="text" label="Current Email" name="current-email" txtVal="user@gmail.com" disabled="true"}}
									{{>component/input type="text" label="New Email" name="new-email"}}
									{{>component/input type="text" label="Confirm New Email" name="confirm-new-email" txterror="New email doesn't match" txthint="New email doesn't match"}}
								</div>
								<div class="user-form-action">
									{{>component/button type="submit" name="btn-save-user" class="-primary" label="Save"}}
								</div>
							</div>		
						</form>
					</div>

				</div>

				<div class="grid-col -aside">
					{{>layout/sidebar}}
				</div>

			</div>

		</div>

		<!-- notify message -->
		{{>module/panel_alert}}

	</div>
	<!-- e: root -->

	<!-- emoji source -->
	<div id="emoji">
		{{>component/emoji}}
	</div>

	<!-- nav add post -->
	{{>module/nav_add_post}}

{{>base/js}}
</body>
</html>
